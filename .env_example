# 模型基本配置
OPENAI_BASE_URL="http://127.0.0.1:11434/v1"
OPENAI_API_KEY="ollama"
AI_MYSISTER="qwen3:8b"

# 长期记忆向量召回：Embedding 模型（OpenAI 兼容 /v1/embeddings）
# 留空 = 关闭向量召回，自动退化为关键词重叠召回（仍可用，只是相关度略差）
# 本地 Ollama 示例：EMBEDDING_MODEL="bge-m3"（需先 ollama pull bge-m3）
EMBEDDING_MODEL=""
# EMBEDDING_BASE_URL="http://127.0.0.1:11434/v1"   # 缺省复用 OPENAI_BASE_URL
# EMBEDDING_API_KEY="ollama"                        # 缺省复用 OPENAI_API_KEY
